# Vercel Blob Storage Configuration
# Copy this file to .env.local and add your actual values

# Vercel Blob Storage Token (required for image migration)
# Get this from your Vercel dashboard > Project Settings > Environment Variables
BLOB_READ_WRITE_TOKEN=your_blob_token_here

# Optional: Custom blob storage URL (auto-detected if not set)
# BLOB_BASE_URL=https://your-custom-blob-url.vercel-storage.com